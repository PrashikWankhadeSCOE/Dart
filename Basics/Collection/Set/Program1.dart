void main() {
  var jerNo = {18, 7, 45, 1};
  print(jerNo);
  print(jerNo.runtimeType);
  /*
  Hash Set used hashTable internally so insertion order is not maintained
  to get the data in same order a inserted we can use linked hash set 
  */
}
